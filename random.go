package main

import (
	"fmt"
	"math/rand"
)

func main() {
	//rand.Seed(time.Now().UnixNano())
	//min := 10
	//max := 30
	//fmt.Println(rand.Intn(max-min+1) + min)
	fmt.Println(rand.Intn(200))
}
